#include <stdio.h>

int main(void)
{
    //Loops through 5 times, almost the exact same as java
    for (int i = 1; i < 6; i++) {
        printf("I like loops in C %d times over because they're similar to Java!\n", i);
    }

    return 0;
}